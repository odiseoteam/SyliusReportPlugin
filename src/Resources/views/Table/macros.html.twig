{% macro list(report, labels, fields, values) %}

{% import 'SyliusResourceBundle:Macros:buttons.html.twig' as buttons %}

{% if values|length > 0 %}
<div class="ui segment">
    <table class="ui sortable stackable celled table">
        <thead>
        <tr>
            <th>{{ 'id' }}</th>
            {% for label in labels %}
                <th>{{ label }}</th>
            {% endfor %}
        </tr>
        </thead>
        <tbody>
        {% for key,value in values %}
            <tr id="{{ loop.index }}">
                <td>{{ loop.index }}</td>
                <td>{{ key }}</td>
                <td>{{ value }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
{% else %}
    <span>{{ 'sylius.ui.there_are_no_reports_created'|trans }}</span>
{% endif %}

{% endmacro %}
